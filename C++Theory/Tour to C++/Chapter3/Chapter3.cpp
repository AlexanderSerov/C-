import std.core;
import bezierMath;
import Foo;

using namespace std;



void main()
{
// *.h or *.hpp header difference
// Modularity. Right now - Textual inclusion(Preprocess, Compile, Link).
// Modilar import (Precompile, Preprocess, Compile, Link)
    /*cout << bernstein(2,2,4.0) << endl;
    cout << "Hello world!!" << endl;
    cout << Bar::f() << endl;*/

// Linkage process.
    // See Bezier curve file

// Use #pragma once as it supported almost all modern compilers.

int a = 3;
}